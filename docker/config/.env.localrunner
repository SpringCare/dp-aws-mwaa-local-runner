# =============================================================================
# MWAA Local Runner Environment Configuration
# =============================================================================
# Copy this file to .env.localrunner and fill in your personal values:
#   cp .env.example .env.localrunner
#
# The .env.localrunner file is gitignored - your personal config stays local.
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: Fill these in with your personal values
# -----------------------------------------------------------------------------

# Your AWS profile name (run `aws sso login --profile <profile>` before starting)
AWS_PROFILE=siham-dev

# Your Snowflake username (usually your email)
SNOWFLAKE_USERNAME=shachi@springhealth.com

# -----------------------------------------------------------------------------
# Optional: Override these if needed
# -----------------------------------------------------------------------------

# Path to dp_airflow_pipelines repo (default: sibling directory)
# Uncomment and set if your repos are not side-by-side
# DP_AIRFLOW_PATH=/path/to/dp_airflow_pipelines

# MWAA environment - determines which secrets/databases to use
# Options: VALIDATION locally
MWAA_ENV=validation

# Execution environment - set to "local" for local development
EXECUTION_ENVIRONMENT=local

# AWS region
AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1

# -----------------------------------------------------------------------------
# Secret Names (usually don't need to change)
# -----------------------------------------------------------------------------
SF_CENSUS_ETL_SECRET_NAME=DE/VALIDATION/sf_credentials_census_etl
SLACK_WEBHOOK_DATAENG_SECRET_NAME=DE/VALIDATION/slack_webhook_airflow_dataeng_alerts

# -----------------------------------------------------------------------------
# Airflow Configuration
# -----------------------------------------------------------------------------

# Default admin password for Airflow UI (admin/admin)
DEFAULT_PASSWORD=admin

# Airflow variables (prefix with AIRFLOW_VAR_)
AIRFLOW_VAR_CENSUS_COMPANIES_REVIEW=test-customer

# Python path for virtualenv tasks to find local modules
PYTHONPATH=/usr/local/airflow

# -----------------------------------------------------------------------------
# SSL Configuration (for Zscaler/corporate proxy)
# -----------------------------------------------------------------------------
REQUESTS_CA_BUNDLE=/etc/pki/tls/certs/ca-bundle.crt
SSL_CERT_FILE=/etc/pki/tls/certs/ca-bundle.crt
CURL_CA_BUNDLE=/etc/pki/tls/certs/ca-bundle.crt
